name: Deploy GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version-file: .node-version
          cache: npm
          cache-dependency-path: web/package-lock.json

      - name: Install frontend dependencies
        run: npm ci
        working-directory: web

      - name: Build validated anonymous bond campaigns
        run: python3 -m pipeline.bond_campaigns

      - name: Build static site
        run: npm run build
        working-directory: web
        env:
          VITE_BASE_PATH: ${{ vars.VITE_BASE_PATH }}
          VITE_LODGING_API_URL: ${{ vars.VITE_LODGING_API_URL }}
          VITE_BOOKING_AFFILIATE_ID: ${{ vars.VITE_BOOKING_AFFILIATE_ID }}
          VITE_AIRBNB_AFFILIATE_TEMPLATE: ${{ vars.VITE_AIRBNB_AFFILIATE_TEMPLATE }}
          VITE_EXPEDIA_AFFILIATE_TEMPLATE: ${{ vars.VITE_EXPEDIA_AFFILIATE_TEMPLATE }}
          VITE_ANALYTICS_COLLECT_URL: ${{ vars.VITE_ANALYTICS_COLLECT_URL }}

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: web/dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
